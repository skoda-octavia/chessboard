import { InfiniteRangePiece } from './infinite-range-piece';

describe('InfiniteRangePiece', () => {
  it('should create an instance', () => {
    expect(new InfiniteRangePiece()).toBeTruthy();
  });
});
